import { useEffect, useState, type ReactElement } from "react";
import { Routes, Route, useNavigate, Navigate } from "react-router-dom";
import "./App.css";import "./premium-theme.css";import "./admin-theme.css";import Home from "./pages/Home";import CustomerAuth from "./pages/CustomerAuth";import Checkout from "./pages/Checkout";import MyOrders from "./pages/MyOrders";import Profile from "./pages/Profile";import Dashboard from "./pages/Dashboard";import Customers from "./pages/admin/Customers";import CoffeeManagement from "./pages/admin/CoffeeManagement";import Orders from "./pages/admin/Orders";import Navbar from "./components/Navbar";import { getCoffees,type Coffee } from "./services/api";
interface CartItem extends Coffee{quantity:number}
function CartDrawer({items,onClose,onIncrease,onDecrease,onRemove}:{items:CartItem[];onClose:()=>void;onIncrease:(id:number)=>void;onDecrease:(id:number)=>void;onRemove:(id:number)=>void}){const navigate=useNavigate();const subtotal=items.reduce((s,i)=>s+i.price*i.quantity,0);const totalItems=items.reduce((s,i)=>s+i.quantity,0);return <><button aria-label="Close cart" onClick={onClose} className="fixed inset-0 z-[60] bg-black/35 backdrop-blur-[2px]"/><aside className="fixed right-0 top-0 z-[70] h-full w-full max-w-md bg-[#fbf7f2] shadow-2xl flex flex-col"><div className="flex items-center justify-between p-6 border-b border-black/10"><div><p className="text-[10px] uppercase tracking-[0.25em] text-[var(--coffee-brown)]">Your order</p><h2 className="text-2xl font-bold coffee-heading mt-1">Coffee Cart</h2></div><button onClick={onClose} className="w-10 h-10 rounded-full border border-black/10 text-xl">×</button></div><div className="flex-1 overflow-y-auto p-6">{items.length===0?<div className="h-full flex flex-col items-center justify-center text-center"><div className="text-7xl mb-5">☕</div><h3 className="text-xl font-bold coffee-heading">Your cart is empty</h3><button onClick={onClose} className="coffee-button mt-6">Browse menu</button></div>:<div className="space-y-4">{items.map(item=><div key={item.id} className="rounded-2xl bg-white border border-black/5 p-4 shadow-sm"><div className="flex gap-4"><div className="w-16 h-16 rounded-xl bg-[#eadccf] flex items-center justify-center text-3xl">☕</div><div className="min-w-0 flex-1"><div className="flex justify-between gap-3"><div><h3 className="font-bold coffee-heading truncate">{item.name}</h3><p className="text-xs coffee-muted mt-1">₹{item.price} each</p></div><button onClick={()=>onRemove(item.id)} className="text-xs text-red-700">Remove</button></div><div className="flex items-center justify-between mt-4"><div className="inline-flex items-center rounded-full border border-black/10 overflow-hidden"><button onClick={()=>onDecrease(item.id)} className="w-9 h-8">−</button><span className="w-8 text-center text-sm font-semibold">{item.quantity}</span><button onClick={()=>onIncrease(item.id)} className="w-9 h-8">+</button></div><span className="font-bold text-[var(--coffee-brown)]">₹{item.price*item.quantity}</span></div></div></div></div>)}</div>}</div>{items.length>0&&<div className="border-t border-black/10 p-6 bg-white"><div className="flex justify-between text-sm coffee-muted"><span>Items</span><span>{totalItems}</span></div><div className="flex justify-between mt-2 text-sm coffee-muted"><span>Subtotal</span><span>₹{subtotal}</span></div><div className="flex justify-between mt-4 pt-4 border-t border-black/10 text-xl font-bold coffee-heading"><span>Total</span><span>₹{subtotal}</span></div><button onClick={()=>{onClose();navigate("/checkout")}} className="coffee-button w-full mt-5 justify-center">Proceed to checkout <span>→</span></button></div>}</aside></>}
function HomePage(){const[coffees,setCoffees]=useState<Coffee[]>([]);const[loading,setLoading]=useState(true);const[error,setError]=useState("");const[cartItems,setCartItems]=useState<CartItem[]>(()=>{try{return JSON.parse(localStorage.getItem("coffeehub_cart")||"[]")}catch{return[]}});const[cartOpen,setCartOpen]=useState(false);const navigate=useNavigate();const loadCoffees=()=>{setLoading(true);setError("");getCoffees().then(setCoffees).catch(()=>setError("Unable to load coffee menu")).finally(()=>setLoading(false))};useEffect(()=>{loadCoffees()},[]);useEffect(()=>{localStorage.setItem("coffeehub_cart",JSON.stringify(cartItems));window.dispatchEvent(new Event("coffeehub:cart-updated"))},[cartItems]);useEffect(()=>{const open=()=>setCartOpen(true);window.addEventListener("coffeehub:open-cart",open);return()=>window.removeEventListener("coffeehub:open-cart",open)},[]);useEffect(()=>{const handler=(event:Event)=>{const coffee=(event as CustomEvent<Coffee>).detail;if(!coffee)return;setCartItems(current=>{const existing=current.find(i=>i.id===coffee.id);return existing?current.map(i=>i.id===coffee.id?{...i,quantity:i.quantity+1}:i):[...current,{...coffee,quantity:1}]});setCartOpen(true)};window.addEventListener("coffeehub:add-to-cart",handler);return()=>window.removeEventListener("coffeehub:add-to-cart",handler)},[]);const increase=(id:number)=>setCartItems(items=>items.map(i=>i.id===id?{...i,quantity:i.quantity+1}:i));const decrease=(id:number)=>setCartItems(items=>items.flatMap(i=>i.id!==id?[i]:i.quantity>1?[{...i,quantity:i.quantity-1}]:[]));const remove=(id:number)=>setCartItems(items=>items.filter(i=>i.id!==id));if(loading)return <div className="coffee-page min-h-screen flex items-center justify-center"><div className="text-center"><div className="text-6xl mb-5 animate-pulse">☕</div><p className="text-[var(--coffee-brown)]">Brewing your coffee menu...</p></div></div>;if(error)return <div className="coffee-page min-h-screen flex items-center justify-center"><div className="coffee-card p-8 text-center"><div className="text-5xl">☕</div><h2 className="text-2xl font-bold coffee-heading mt-4">Something went wrong</h2><p className="coffee-muted mt-3">{error}</p><button onClick={loadCoffees} className="coffee-button mt-6">Try Again</button></div></div>;return <><Navbar/><Home coffees={coffees} onLogin={()=>navigate("/customer-auth")}/>{cartOpen&&<CartDrawer items={cartItems} onClose={()=>setCartOpen(false)} onIncrease={increase} onDecrease={decrease} onRemove={remove}/>}</>}
function isAdminToken(){const token=localStorage.getItem("access_token");if(!token)return false;try{const payload=token.split(".")[1];if(!payload)return false;const normalized=payload.replace(/-/g,"+").replace(/_/g,"/");const json=decodeURIComponent(atob(normalized.padEnd(normalized.length+((4-normalized.length%4)%4),"=")).split("").map(char=>`%${(`00${char.charCodeAt(0).toString(16)}`).slice(-2)}`).join(""));return JSON.parse(json).role==="admin"}catch{return false}}
function ProtectedAdminPage({children}:{children:ReactElement}){return isAdminToken()?<AdminShell>{children}</AdminShell>:<Navigate to="/customer-auth" replace/>}function ProtectedProfile(){return localStorage.getItem("customer_access_token")?<Profile/>:<Navigate to="/customer-auth" replace/>}function AdminShell({children}:{children:ReactElement}){return <div className="coffee-admin-shell">{children}</div>}
function App(){return <Routes><Route path="/" element={<HomePage/>}/><Route path="/login" element={<Navigate to="/customer-auth" replace/>}/><Route path="/customer-auth" element={<CustomerAuth/>}/><Route path="/checkout" element={<Checkout/>}/><Route path="/my-orders" element={localStorage.getItem("customer_access_token")?<MyOrders/>:<Navigate to="/customer-auth" replace/>}/><Route path="/profile" element={<ProtectedProfile/>}/><Route path="/dashboard" element={<ProtectedAdminPage><Dashboard/></ProtectedAdminPage>}/><Route path="/customers" element={<ProtectedAdminPage><Customers/></ProtectedAdminPage>}/><Route path="/coffees" element={<ProtectedAdminPage><CoffeeManagement/></ProtectedAdminPage>}/><Route path="/admin/orders" element={<ProtectedAdminPage><Orders/></ProtectedAdminPage>}/><Route path="*" element={<Navigate to="/" replace/>}/></Routes>}
export default App;
