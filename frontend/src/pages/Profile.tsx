import { useEffect, useState, type FormEvent } from "react";
import { useNavigate } from "react-router-dom";
import { getCustomerProfile, updateCustomerProfile, type CustomerProfile } from "../services/api";

function Profile() {
  const navigate = useNavigate(); const [profile, setProfile] = useState<CustomerProfile | null>(null); const [name, setName] = useState(""); const [email, setEmail] = useState(""); const [phone, setPhone] = useState(""); const [address, setAddress] = useState(""); const [city, setCity] = useState(""); const [pincode, setPincode] = useState(""); const [loading, setLoading] = useState(true); const [saving, setSaving] = useState(false); const [message, setMessage] = useState(""); const [error, setError] = useState("");
  useEffect(() => { if (!localStorage.getItem("customer_access_token")) { navigate("/customer-auth", { replace: true }); return; } getCustomerProfile().then((data) => { setProfile(data); setName(data.name); setEmail(data.email); setPhone(data.phone || ""); setAddress(data.address || ""); setCity(data.city || ""); setPincode(data.pincode || ""); }).catch((err) => { const status = (err as Error & { status?: number }).status; if (status === 401) { localStorage.removeItem("customer_access_token"); localStorage.removeItem("customer"); navigate("/customer-auth", { replace: true }); } else setError(err instanceof Error ? err.message : "Unable to load profile"); }).finally(() => setLoading(false)); }, [navigate]);
  const save = async (event: FormEvent) => { event.preventDefault(); setError(""); setMessage(""); setSaving(true); try { const data = await updateCustomerProfile({ name: name.trim(), email: email.trim().toLowerCase(), phone: phone || null, address: address || null, city: city || null, pincode: pincode || null }); setProfile(data); setMessage("Profile and delivery address updated successfully."); localStorage.setItem("customer", JSON.stringify({ id: data.id, name: data.name, email: data.email, phone: data.phone, address: data.address, city: data.city, pincode: data.pincode })); window.dispatchEvent(new Event("coffeehub:customer-auth")); } catch (err) { const status = (err as Error & { status?: number }).status; if (status === 401) { localStorage.removeItem("customer_access_token"); localStorage.removeItem("customer"); navigate("/customer-auth", { replace: true }); } else setError(err instanceof Error ? err.message : "Unable to update profile"); } finally { setSaving(false); } };
  if (loading) return <main className="coffee-page min-h-screen flex items-center justify-center"><p className="coffee-muted">Loading your profile...</p></main>;
  return <main className="coffee-page min-h-screen px-5 py-12 sm:py-16"><div className="max-w-4xl mx-auto"><button onClick={() => navigate("/")} className="coffee-muted hover:text-[var(--coffee-brown)] mb-7">← Back to CoffeeHub</button><section className="coffee-card bg-white p-7 sm:p-10 shadow-xl"><div className="flex items-start justify-between gap-5 mb-8"><div><p className="text-[10px] uppercase tracking-[0.3em] text-[var(--coffee-brown)]">My account</p><h1 className="text-4xl font-bold coffee-heading mt-2">Your profile.</h1><p className="coffee-muted mt-2">Keep your contact and delivery details ready for faster checkout.</p></div><div className="w-14 h-14 rounded-full bg-[var(--coffee-dark)] text-white flex items-center justify-center text-xl">☕</div></div>{profile && <form onSubmit={save} className="space-y-7"><div><p className="text-xs uppercase tracking-[0.2em] font-semibold text-[var(--coffee-brown)] mb-4">Personal details</p><div className="grid sm:grid-cols-2 gap-5"><div><label className="coffee-label">Full name</label><input className="coffee-input" value={name} onChange={(e) => setName(e.target.value)} required minLength={2} /></div><div><label className="coffee-label">Phone</label><input className="coffee-input" value={phone} onChange={(e) => setPhone(e.target.value.replace(/\D/g, "").slice(0, 10))} placeholder="9876543210" inputMode="tel" /></div><div className="sm:col-span-2"><label className="coffee-label">Email</label><input className="coffee-input" type="email" value={email} onChange={(e) => setEmail(e.target.value)} required /></div></div></div><div className="border-t border-black/10 pt-7"><p className="text-xs uppercase tracking-[0.2em] font-semibold text-[var(--coffee-brown)] mb-4">Delivery address</p><div className="grid sm:grid-cols-2 gap-5"><div className="sm:col-span-2"><label className="coffee-label">Address</label><textarea className="coffee-input min-h-28 resize-none" value={address} onChange={(e) => setAddress(e.target.value)} placeholder="House / Flat, street, area" /></div><div><label className="coffee-label">City</label><input className="coffee-input" value={city} onChange={(e) => setCity(e.target.value)} placeholder="Your city" /></div><div><label className="coffee-label">Pincode</label><input className="coffee-input" inputMode="numeric" maxLength={6} value={pincode} onChange={(e) => setPincode(e.target.value.replace(/\D/g, "").slice(0, 6))} placeholder="520001" /></div></div><p className="text-xs coffee-muted mt-3">This address will be pre-filled during checkout.</p></div>{error && <div className="rounded-xl bg-red-50 border border-red-100 text-red-700 px-4 py-3 text-sm">{error}</div>}{message && <div className="rounded-xl bg-green-50 border border-green-100 text-green-700 px-4 py-3 text-sm">{message}</div>}<div className="flex flex-wrap gap-3 pt-2"><button disabled={saving} className="coffee-button disabled:opacity-50" type="submit">{saving ? "Saving..." : "Save changes →"}</button><button type="button" onClick={() => navigate("/my-orders")} className="px-5 py-3 rounded-full border border-black/10 font-semibold">View my orders</button></div></form>}</section></div></main>;
}
export default Profile;
