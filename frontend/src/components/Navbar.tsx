import { useEffect, useState } from "react";
import { useNavigate } from "react-router-dom";
function readCartCount(){try{return JSON.parse(localStorage.getItem("coffeehub_cart")||"[]").reduce((s:number,i:{quantity:number})=>s+i.quantity,0)}catch{return 0}}
function Navbar(){const navigate=useNavigate();const[customer,setCustomer]=useState<{name:string;email:string}|null>(()=>{try{return JSON.parse(localStorage.getItem("customer")||"null")}catch{return null}});const[cartCount,setCartCount]=useState(readCartCount());useEffect(()=>{const sync=()=>{try{setCustomer(JSON.parse(localStorage.getItem("customer")||"null"))}catch{setCustomer(null)}};const cart=()=>setCartCount(readCartCount());window.addEventListener("coffeehub:customer-auth",sync);window.addEventListener("storage",sync);window.addEventListener("coffeehub:cart-updated",cart);return()=>{window.removeEventListener("coffeehub:customer-auth",sync);window.removeEventListener("storage",sync);window.removeEventListener("coffeehub:cart-updated",cart)}},[]);const logout=()=>{localStorage.removeItem("customer_access_token");localStorage.removeItem("customer");setCustomer(null);navigate("/")};const go=(id:string)=>{const el=document.getElementById(id);if(el)el.scrollIntoView({behavior:"smooth"});else navigate(id==="menu"?"/menu":"/")};const openCart=()=>window.dispatchEvent(new Event("coffeehub:open-cart"));return <nav className="reference-nav"><button className="reference-brand" onClick={()=>navigate("/")} aria-label="CoffeeHub home"><span className="reference-brand-mark">☕</span><span><strong>CoffeeHub</strong><small>Specialty Coffee</small></span></button><div className="reference-nav-links"><button onClick={()=>navigate("/")}>Home</button><button onClick={()=>go("menu")}>Menu</button><button onClick={()=>go("story")}>Our Story</button><button onClick={()=>go("popular")}>News</button><button onClick={()=>go("contact")}>Contact</button></div><div className="reference-nav-actions"><button className="reference-icon" onClick={()=>navigate("/menu")} aria-label="Search">⌕</button><button className="reference-icon cart" onClick={openCart} aria-label="Open cart">🛒{cartCount>0&&<span>{cartCount}</span>}</button>{customer?<><button className="reference-user" onClick={()=>navigate("/profile")}><span>{customer.name.charAt(0).toUpperCase()}</span>{customer.name.split(" ")[0]}</button><button className="reference-signin" onClick={logout}>Logout</button></>:<button className="reference-signin" onClick={()=>navigate("/customer-auth")}>♙ &nbsp; Sign in</button>}</div><button className="reference-mobile" onClick={()=>navigate(customer?"/profile":"/customer-auth")}>{customer?customer.name.charAt(0).toUpperCase():"→"}</button></nav>}
export default Navbar;
